# 4.  best time to buy and sell stock

# 1st way

# we solve using two pointer methods.
# l --> buy (less)    buy at less rate
# r --> sell (high)  sell at high rate

"""
def max_profit(prices):
    l = r = 0  # l -> buy and r -> sell
    max_pro = 0

    while r  < len(prices):
        if prices[l] < prices[r]:                # if buy < sell
            profit = prices[r] - prices[l]       # p = buy - sell
            max_pro = max( max_pro , profit )    # select max among these.
        else :
            l = r
        r+=1
    return max_pro
prices = [7,1,5,3,6,4]
print(max_profit(prices))
"""

# 2nd way

"""
def maxProfit(self, prices):
    min_price = prices[0]
    max_profit = 0

    for price in prices[1:]:
        max_profit = max(max_profit, price - min_price)
        min_price = min(min_price, price)

    return max_profit
"""
