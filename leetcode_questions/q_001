# 1. two sum  
# https://leetcode.com/problems/two-sum/


# 1st way ( brute force method )

"""
l = [2,1,5,3]
tar = 7

for i in range(len(l)):
    for j in range(i+1,len(l)):
        if l[i] + l[j] == tar:
            print((f'{(i,j)} are the index of {l[i]} and {l[j]} where meets the total'))
"""


# 2nd way ( array and hash table)

"""
l = [4,3,6,1,9,2]
tar = 11

seen = {}
for i in range(len(l)):
    need = tar - l[i]         # to 1st element and store

    if need in seen:
        print(seen[need] , i )
    seen[l[i]] = i                          # finally add all in seen. In dict ,  keys are unique.

"""
