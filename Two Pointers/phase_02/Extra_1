

# Q 2460. apply operation to an array

# 1st way -> use n-1 like given in question to check in first for loop

"""
nums = [1,2,2,1,1,0]                         # Output: [1,4,2,0,0,0]

for i in range(1,len(nums)):
    if nums[i] != nums[i-1]:
        continue
    else:
        nums[i-1] = nums[i-1]*2
        nums[i] = 0
        i+=1

l = 0
for r in range(len(nums)):
    if nums[r] != 0 :
        nums[l],nums[r] = nums[r],nums[l]
        l+=1
print(nums)
"""

# ----------------------------------------------------------------------------------------------------------------------

# 2nd way --> use i+1 in first loop and remove just the non zero from array and add exact length required in last.

"""
nums = [1,2,2,1,1,0]

for i in range(len(nums)-1):
    if nums[i] == nums[i+1]:
        nums[i] = nums[i] * 2
        nums[i+1] = 0

l = 0
for r in range(len(nums)):
    if nums[r] != 0:
        nums[l] = nums[r]
        l+=1
print(nums[:l] + [0] * (len(nums) - l))
"""

# ----------------------------------------------------------------------------------------------------------------------

# 3rd way -> use extra variable to move zero and replace back for inplace
#            no use of else , coz the list already with 0 , so no need to change like tae r at end and reduce.

"""
# nums = [1,2,2,1,1,0]

nums = [0, 1, 0, 3, 12]

for i in range(len(nums)-1):
    if nums[i] == nums[i+1]:
        nums[i] = nums[i] * 2
        nums[i+1] = 0

out=[0]*len(nums)

l = 0
for j in nums:
    if j != 0:
        out[l] = j
        l+=1

nums[:] = out
print(nums)
"""

# ----------------------------------------------------------------------------------------------------------------------
