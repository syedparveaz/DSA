

# 11. 26   - Remove Duplicates from Sorted Array — (Array)

"""
nums = [1,1,2,2,2,3,3,4]

l = 1
for r in range(1,len(nums)):
    if nums[r] == nums[r-1]:
        continue
    else:
        nums[l] = nums[r]
        l+=1
print(l,nums)
"""


# --------------------------------------------------------------------------------------------------------

# by typecasting.

"""
nums = [1,1,2,2,2,3,3,4]
# print(nums)
# print(id(nums))
nums[:] = sorted(list(set(nums)))        # to make it in place , use nums[:], using sort coz set is unordered datatype.
# print(nums)
# print(id(nums))
print(len(nums))      # actual output is len(nums) after removing duplicates inplace..
"""

